
version: '3.4'

services:
    db:
        image: postgres
        restart: always
        environment:
            - POSTGRES_USER=productcatalog
            - POSTGRES_PASSWORD=productcatalog
        ports:
            - '5432:5432'

    adminer:
        image: adminer
        restart: always
        ports:
            - "8080:8080"
        depends_on:
            - "db"
